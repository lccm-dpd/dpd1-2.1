'use strict';

const _setup = () => {
  const r = 255;
  const g = 0;
  const b = 0;

  createCanvas(windowWidth,windowHeight);

  background(r, g, b);
};

const _draw = () => {
  // empty for now
};
